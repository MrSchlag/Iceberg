using System;
using System.Collections.Generic;

public static class RadianCircleProvider
{
    public static List<float> RadianCircle = new List<float>
    {
        (float)Math.PI * 2,
        (float)Math.PI / 12,
        (float)Math.PI / 6,
        (float)Math.PI / 4,
        (float)(Math.PI * 5) / 12,
        (float)Math.PI / 2,
        (float)(Math.PI * 7) / 12,
        (float)(Math.PI * 2) / 3,
        (float)(Math.PI * 3) / 4,
        (float)(Math.PI * 5) / 6,
        (float)(Math.PI * 11) / 12,
        (float)Math.PI,
        (float)(Math.PI * 13) / 12,
        (float)(Math.PI * 7) / 6,
        (float)(Math.PI * 5) / 4,
        (float)(Math.PI * 4) / 3,
        (float)(Math.PI * 17) / 12,
        (float)(Math.PI * 3) / 2,
        (float)(Math.PI * 19) / 12,
        (float)(Math.PI * 5) / 3,
        (float)(Math.PI * 7) / 4,
        (float)(Math.PI * 11) / 6,
        (float)(Math.PI * 23) / 12
    };
}